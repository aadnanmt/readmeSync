query {
  viewer {
    repositories(first: 100, ownerAffiliations: OWNER, isFork: true, privacy: PUBLIC) {
      nodes {
        languages(first: 10, orderBy: {field: SIZE, direction: DESC}) {
          edges {
            size
            node {
              name
            }
          }
        }
      }
    }
  }
}
