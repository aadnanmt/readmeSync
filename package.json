{
  "name": "readmeSync",
  "type": "module",
  "scripts": {
    "start": "bun scripts/index.ts",
    "run": "bun scripts/index.ts ./README.md ./stats.json",
    "format": "prettier . --write",
    "setup": "cp .env.example .env || copy .env.example .env"
  },
  "dependencies": {
    "@types/node": "^25.5.2"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
